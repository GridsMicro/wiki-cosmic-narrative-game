# 📚 DevTools คู่มือผู้พัฒนา

## 🎯 จุดประสงค์
DevTools เป็นอินเทอร์เฟซสำหรับผู้ดูแลเกม (admin) เพื่อจัดการข้อมูลในฐานข้อมูล Supabase อย่างง่ายดายโดยไม่ต้องเขียน SQL หรือใช้เครื่องมือภายนอก.  คุณสามารถ **ดู**, **เพิ่ม**, **แก้ไข**, **ลบ** ข้อมูลของตารางต่าง ๆ เช่น `cards_master`, `oracle_prophecies`, `characters_master`, `announcements` ฯลฯ.

## 🗂️ โครงสร้างไฟล์สำคัญ
```
app/
 └─ game/
     └─ dev-tools/
         ├─ page.tsx            # หน้า DevTools หลัก (router /game/dev-tools)
         ├─ config/
         │    └─ menuConfig.tsx   # กำหนดเมนูและฟอร์มที่แสดงใน DevTools
         └─ forms/
              ├─ CardsMasterForm.tsx
              ├─ OraclePropheciesForm.tsx
              ├─ AnnouncementsForm.tsx   # ฟอร์มใหม่ที่เราเพิ่งเพิ่ม
              └─ ... (ฟอร์มอื่น ๆ)
```

## 🚀 วิธีเปิด DevTools
1. **เข้าสู่ระบบ** – ต้องมีผู้ใช้ที่ล็อกอิน (Supabase auth) และมี role `admin` (หรือคุณสามารถตั้งค่า RLS ให้เปิดให้ทุกคนเข้าถึงในระหว่างพัฒนา).
2. ไปที่ URL: `https://your-domain.com/game/dev-tools` (หรือ `http://localhost:3000/game/dev-tools` ในโหมดพัฒนา).
3. หน้าแสดงเมนูด้านซ้ายตามที่กำหนดใน `menuConfig.tsx`.

## 📋 เมนูและฟอร์ม
- **เมนู** ถูกกำหนดใน `app/game/dev-tools/config/menuConfig.tsx` ด้วยโครงสร้าง:
  ```tsx
  export const MENU_CONFIG = [
    {
      id: 'cards_master',
      label: 'Cards',
      icon: <CreditCard size={14} />,   // ไอคอนจาก lucide-react
      form: CardsMasterForm,            // คอมโพเนนท์ฟอร์ม
      imageField: 'image_url',
    },
    // …อื่น ๆ
    {
      id: 'announcements',
      label: 'Announcements',
      icon: <Megaphone size={14} />,   // ไอคอนใหม่
      form: AnnouncementsForm,
      numberFields: ['priority', 'repeat_interval_hours'],
      booleanFields: ['is_active', 'is_recurring', 'show_on_login', 'show_as_popup'],
    },
  ];
  ```
- **ฟอร์ม** อยู่ใน `app/game/dev-tools/forms/`.  แต่ละฟอร์มรับ `editingItem`, `onSubmit`, `onUpload` เป็น props และใช้คอมโพเนนท์ UI ร่วมจาก `SharedUI.tsx` (Input, Select, Textarea, SubmitBtn).

## 🛠️ การเพิ่มเมนูใหม่
1. **สร้างฟอร์ม** – สร้างไฟล์ใหม่ใน `forms/` เช่น `MyNewTableForm.tsx` และใช้คอมโพเนนท์ UI ตามตัวอย่าง.
2. **อัปเดต `menuConfig.tsx`** – เพิ่มอ็อบเจ็กต์ใหม่ใน `MENU_CONFIG` โดยระบุ:
   - `id` – ชื่อ table ใน Supabase (ต้องตรงกับชื่อ table).
   - `label` – ชื่อเมนูที่แสดง.
   - `icon` – ไอคอนจาก `lucide-react`.
   - `form` – คอมโพเนนท์ฟอร์มที่สร้าง.
   - (optional) `imageField`, `jsonFields`, `numberFields`, `booleanFields` – ชื่อคอลัมน์ที่ต้องการจัดการพิเศษ.
3. **รีสตาร์ท dev server** (`npm run dev`) เพื่อให้การเปลี่ยนแปลงโหลด.

## 📦 การเชื่อมต่อ Supabase
- ไฟล์ `lib/supabase.ts` ทำการสร้าง client:
  ```ts
  import { createClient } from '@supabase/supabase-js';
  export const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
  ```
- ทุกฟอร์มใช้ `supabase.from(tableId).select()`, `insert()`, `update()`, `delete()` ผ่าน `fetchData`, `handleSave`, `handleDelete` ที่กำหนดใน `page.tsx`.

## 🌐 Environment Variables

### การแยก Host (Development)
ถ้าคุณแยกโปรเจกต์เป็น 2 ส่วน:
- **`engins-cosmic-narrative-game`** - DevTools (localhost:3000)
- **`cosmic-narrative-game`** - เกมจริง (localhost:3002)

ต้องเพิ่ม environment variable ใน `.env.local` ของ DevTools:

```env
# URL ของโปรเจกต์เกมจริง (cosmic-narrative-game)
NEXT_PUBLIC_GAME_URL=http://localhost:3002
```

### การขึ้น Production
เมื่อขึ้น production ให้เปลี่ยน URL เป็นโดเมนจริง:

```env
# Production
NEXT_PUBLIC_GAME_URL=https://yourgame.com
```

### ตัวอย่างไฟล์ `.env.local` สมบูรณ์:
```env
# Supabase Connection
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# URL ของโปรเจกต์เกมจริง
NEXT_PUBLIC_GAME_URL=http://localhost:3002

# Gemini API (ถ้ามี)
GEMINI_API_KEY=your-api-key-here
```

### การใช้งาน
ปุ่ม "Visit" (รูปตา 👁️) ใน DevTools จะใช้ `NEXT_PUBLIC_GAME_URL` เพื่อ redirect ไปยังหน้าเกมจริง:

```tsx
const gameUrl = process.env.NEXT_PUBLIC_GAME_URL || 'http://localhost:3000';
window.location.href = `${gameUrl}/game/world/${item.slug}`;
```

**หมายเหตุ:** หลังจากเปลี่ยน `.env.local` ต้อง **restart dev server** (`npm run dev`) เพื่อให้มีผล


## 🧭 ตัวอย่างการใช้งาน
### ดูข้อมูล
```tsx
const { data, error } = await supabase.from('announcements').select('*');
```
### เพิ่มรายการใหม่
```tsx
await supabase.from('announcements').insert({
  title: 'New Event',
  message: 'Event starts tomorrow!',
  type: 'event',
  priority: 1,
  start_time: new Date().toISOString(),
  is_recurring: false,
});
```
### แก้ไขรายการ
```tsx
await supabase.from('announcements')
  .update({ title: 'Updated Event' })
  .eq('id', announcementId);
```
### ลบรายการ
```tsx
await supabase.from('announcements').delete().eq('id', announcementId);
```

## 📖 คำแนะนำเพิ่มเติม
- **RLS (Row Level Security)** – ใน production ควรเปิด RLS และกำหนด policy ให้ admin เท่านั้นที่สามารถ `INSERT/UPDATE/DELETE` ตาราง admin‑only.
- **การอัปเดต UI** – หลังจากบันทึกหรือทำการลบ ให้เรียก `fetchData()` เพื่อรีเฟรชตาราง.
- **การอัปโหลดไฟล์** – ฟอร์มที่มี `imageField` จะใช้ `onUpload` เพื่ออัปโหลดไฟล์ไปยัง Supabase storage (ดู `SharedUI.tsx` สำหรับตัวอย่าง).

---

**ทำไมคุณไม่เจอคู่มือ?**
- ก่อนหน้านี้ไม่มีไฟล์คู่มือ DevTools ใน wiki ของโปรเจกต์.  เราได้สร้างไฟล์นี้เพื่อให้คุณและทีมงานสามารถอ้างอิงได้ทันที.
- หากต้องการเพิ่มรายละเอียดเพิ่มเติม (เช่น workflow การ deploy, การตั้งค่า RLS) คุณสามารถแก้ไขไฟล์นี้ได้โดยตรงใน `wiki-cosmic-narrative-game/docs/developer/dev-tools.md`.

**อ้างอิง**
- `docs/dev-tools-add-menu.md` – ตัวอย่างการเพิ่มเมนูใหม่ (ยังเป็นร่าง).
- `app/game/dev-tools/config/menuConfig.tsx` – โค้ดกำหนดเมนูปัจจุบัน.
- `app/game/dev-tools/forms/AnnouncementsForm.tsx` – ฟอร์มประกาศใหม่.

---

*อัพเดตเมื่อ:* 2025‑12‑27  
*ผู้จัดทำ:* Antigravity (AI)  
*เพิ่มเติม:* Environment Variables สำหรับการแยก host และ production deployment
   
