# โครงสร้างโปรเจค (Project Structure)

บันทึกโครงสร้างสถาปัตยกรรมของ Cosmic Narrative Game (อัพเดทล่าสุด 2025-12-21) เพื่อความเข้าใจในการพัฒนาและรักษาโค้ด

---

## 🏗️ ภาพรวมสถาปัตยกรรม

โปรเจคนี้ใช้โครงสร้าง **Modular Monolith** บน Next.js โดยแยกส่วน Logic ของ AI Chat และ RPG ออกจากกันแต่แชร์ State ร่วมกันผ่าน `GameContext`

---

## 📂 โฟลเดอร์หลัก

### 🌍 `/app` (Routing & Pages)
- **`/ai`**: หัวใจของระบบ Chat-driven Storytelling ใช้ Gemini API ในการขับเคลื่อนเนื้อเรื่อง
- **`/game`**: ระบบ RPG แบบดั้งเดิม (Traditional RPG)
    - **`layout.tsx`**: จัดการโครงสร้าง Modal และเป็นตัวกลางในการ Sync ข้อมูลระหว่างแชทกับสถานะเกม
    - **`/battle`**: กลไกการต่อสู้แบบเทิร์นเบส
    - **`/cards`**: ระบบจัดการสำรับการ์ดของผู้เล่น
    - **`/dev-tools`**: แผงควบคุมสำหรับนักพัฒนา (ใช้สำหรับ Seed ข้อมูลและจัดการฐานข้อมูลหน้างาน)
- **`/dev`**: ห้องทดลองสำหรับฟีเจอร์ใหม่ๆ ก่อนนำไปใช้จริงในเกม

### 🧩 `/components` (UI/UX)
- **`/game`**: คอมโพเนนต์ที่ใช้เฉพาะในโหมด RPG
    - **`/modals`**: ระบบหน้าต่างป๊อปอัพ (Status, Equipment, Inventory) ที่ถูกแยกเป็นส่วนๆ เพื่อความง่ายในการแก้ไข
    - **`PlayerHUD.tsx`**: แถบแสดงสถานะแบบ Premium (HP, MP, Level, Gold)
    - **`PetCompanion.tsx`**: ระบบสัตว์เลี้ยงที่จะคอยลอยอยู่ข้างหน้าจอ

### 💾 `/context` & `/hooks` (Logic)
- **`GameContext.tsx`**: เป็น **Source of Truth** เพียงหนึ่งเดียวของเกม เชื่อมต่อกับ Supabase ตลอดเวลา
- **`useGameData.ts`**: Hook ส่วนกลางที่ใช้ในการ Read/Write ข้อมูลผู้เล่น เพื่อลดความซ้ำซ้อนของโค้ด

---

## 🗄️ ระบบฐานข้อมูล (Supabase)

เราแยกข้อมูลออกเป็น 3 กลุ่มหลัก:
1. **Master Data**: ข้อมูลที่ห้ามแก้ไขจากฝั่งผู้เล่น (`cards_master`, `items`, `npcs_master`, `maps`)
2. **Player Data**: ข้อมูลส่วนตัวและความคืบหน้า (`player_save_data`)
3. **Instance Data**: สิ่งของที่ผู้เล่นครอบครอง (`player_items`, `player_pets`, `player_cards`)

---

## 🔗 ลิงก์ repository

- **Game Repo**: [GridsMicro/cosmic-narrative-game](https://github.com/GridsMicro/cosmic-narrative-game)
- **Wiki Repo**: [GridsMicro/wiki-cosmic-narrative-game](https://github.com/GridsMicro/wiki-cosmic-narrative-game)

---

!!! info "การอัพเดทโครงสร้าง"
    เราได้นำ `astra-city/` ออกแล้ว และเปลี่ยนมาใช้ระบบ **Dynamic Location** ที่ดึงข้อมูลจากตาราง `maps` แทน เพื่อความยืดหยุ่นในการเพิ่มเมืองใหม่ๆ ในอนาคต
